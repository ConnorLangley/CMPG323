@page
@model ModerationModel
@{
    
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Preview Page 2</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .container {
            text-align: center;
        }

        .file-preview {
            width: 400px;
            height: 250px;
            border: 1px solid #ccc;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f9f9f9;
        }

        button {
            padding: 10px 20px;
            margin: 10px;
            cursor: pointer;
            font-size: 16px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
        }

            button:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>

    <div class="container">
        <!-- File Preview Section -->
        <div class="file-preview">
            <p>File Preview Here</p>
        </div>

        <!-- Submit Button -->
        <button runat="server" id="submit-btn" onclick="AllowClick()">Allow</button>

        <!-- Cancel Button -->
        <button runat="server" id="cancel-btn" onClick="DeleteClick()">Delete</button>

        <!-- Link back to Page 1 -->
        <div>
            <a href="FilePreview.html">Back to Page 1</a>
        </div>
    </div>

</body>
</html>

